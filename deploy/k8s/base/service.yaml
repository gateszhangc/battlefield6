apiVersion: v1
kind: Service
metadata:
  name: battlefield6
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: battlefield6
  ports:
    - name: http
      port: 80
      targetPort: http
